# The flat `foo.bar = …; foo.baz = …;` style is deliberate throughout the
# modules, and hardware-configuration.nix is generated by nixos-generate-config.
disabled = ["repeated_keys"]
ignore = ["hardware-configuration.nix"]
